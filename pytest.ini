[pytest]
markers =
	live_slack: Live Slack API tests (opt-in via env flags)
	live_sheets: Live Google Sheets tests (opt-in via env flags)
	live_ocr: Live OCR tests using system Tesseract (opt-in via env flags)
	live_e2e: Live end-to-end test from Slack URL to Sheets (opt-in via env flags) 